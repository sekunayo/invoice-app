import { css } from "@emotion/css";

const overlay = css({
    backgroundColor: "rgba(0, 0, 0, 0.5)",
    width: "100%",
    height: "100%",
    zIndex: "60",
    position: "absolute",
    top: "0",
    left: "0"
})

export const styles = {
    overlay
}